services:
  carla:
    image: carlasim/carla:0.9.15
    runtime: nvidia
    privileged: true
    stdin_open: true
    tty: true
    network_mode: "host"
    environment:
      - DISPLAY=${DISPLAY}
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - QT_X11_NO_MITSHM=1 # Fix a bug with QT
      - SDL_VIDEODRIVER=x11
    volumes:
      - ${HOME}/.Xauthority:/root/.Xauthority:rw
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./asound.conf:/etc/asound.conf
      # - carla-data:/home/carla
    command: ["bash", "./CarlaUE4.sh", "-vulkan", "-quality-level=Low", "-fps=5", "-carla-port=2000"]

  caraware:
    build:
      context: .
      dockerfile: Dockerfile
    image: hbarros1/caraware:1.1.0
    # runtime: nvidia
    privileged: true
    network_mode: "host"
    environment:
      - CARLA_ROOT=/home/carla
      - PYTHONPATH=/home/carla/PythonAPI/carla/dist/carla-0.9.15-py3.7-linux-x86_64.egg
      - DISPLAY=${DISPLAY}
      - XAUTHORITY=/root/.Xauthority
      # - NVIDIA_VISIBLE_DEVICES=all
      # - NVIDIA_DRIVER_CAPABILITIES=all
      - QT_X11_NO_MITSHM=1 # Fix a bug with QT
      - SDL_VIDEODRIVER=x11
    working_dir: /app
    user: 1000:1000
    volumes:
      - .:/app
      - carla-data:/home/carla
      - ${HOME}/.Xauthority:/root/.Xauthority:rw
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./asound.conf:/etc/asound.conf
      - /etc/passwd:/etc/passwd:ro
      - /etc/group:/etc/group:ro
      - ${HOME}:${HOME}
    command: tail -F anything

volumes:
  carla-data:
    external: true